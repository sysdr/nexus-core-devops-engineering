[package]
name = "orchestrator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.54", default-features = false, features = ["macros"] }

[profile.release]
opt-level = "s"
